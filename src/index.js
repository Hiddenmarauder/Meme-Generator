import React from 'react';
import ReactDOM from 'react-dom';
import MemeGenerator from '../MemeGenerator'; // Assuming you have created the MemeGenerator component

ReactDOM.render(
  <React.StrictMode>
    <MemeGenerator />
  </React.StrictMode>,
  document.getElementById('root')
);
